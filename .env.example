# ===========================================
# Al Haram Travel – Vercel / Hostinger SMTP
# ===========================================
# Local: copy to .env.local and fill in EMAIL_PASS.
# Vercel: Project → Settings → Environment Variables → add each variable below
#         (Production, Preview, Development). No need to "import" a file—add them manually.

# --- Required: SMTP (Hostinger) ---
# SMTP server (Hostinger)
EMAIL_HOST=smtp.hostinger.com

# SMTP port (465 = SSL, 587 = TLS)
EMAIL_PORT=465

# Sender login (your Hostinger email)
EMAIL_USER=info@alharamtravel.uk

# Sender password (app password or email password)
EMAIL_PASS=your-email-password

# --- Optional: Recipients ---
# Where contact form enquiries are sent (defaults to EMAIL_USER if not set)
ADMIN_EMAIL=info@alharamtravel.uk

# ===========================================
# Behaviour
# ===========================================
# 1. Enquiry email → sent to ADMIN_EMAIL (you).
# 2. Confirmation email → sent to the user who submitted the form.
# Both are sent using EMAIL_USER / EMAIL_PASS from your Hostinger account.
