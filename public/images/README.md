# Images

- **placeholder.svg** – Shown when a package image fails to load.
- To use your own photos for a destination, add files here (e.g. `packages/dubai.jpg`) and update the package `image` field in `lib/data.ts` to use `/images/packages/dubai.jpg`.
